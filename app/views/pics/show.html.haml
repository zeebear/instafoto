= image_tag @pic.photo.url
%h1= @pic.title

%p= @pic.description

%p
Foto by
= @pic.user.email

%br

= link_to "Back", root_path
= link_to 'Edit', edit_pic_path
= link_to 'Delete', root_path, method: :delete, data: { confirm: "Are you sure you want to delete this pic?"}
